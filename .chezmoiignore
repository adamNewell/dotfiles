# System-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp
*.swp
*.swo
*~

# Cache directories
.cache/
.npm/
.yarn/
node_modules/

# IDE files
.vscode/
.idea/
*.sublime-*

# OS-specific files
{{- if eq .chezmoi.os "darwin" }}
.CFUserTextEncoding
{{- end }}

{{- if eq .chezmoi.os "linux" }}
.xsession-errors
{{- end }}

# Personal sensitive files (should use encryption instead)
.env
.env.local
.env.production
.env.development
*.key
*.pem
*.p12
*.crt
*.csr
*.token
*_token
*_key
*secret*

# Database files
*.db
*.sqlite
*.sqlite3

# Log files
*.log

# Backup files
*.backup
*.bak

# Git files (managed separately or encrypted)
.gitconfig
.gitignore_global

# Zsh files (handled by ZDOTDIR)
.zshrc

# Tool version files (using mise config.toml instead)
.tool-versions

# Zsh session files (stored in XDG state directory)
.zsh_sessions

# SSH keys (should be encrypted with age)
.ssh/

# Examples of encrypted file naming:
# Instead of: .ssh/config → Use: private_encrypted_dot_ssh/config.age
# Instead of: .aws/credentials → Use: private_encrypted_dot_aws/credentials.age
# Instead of: .env → Use: private_encrypted_env.age

# Repository files that should not be copied to home directory
README.md
LICENSE
*.md
docs/
.github/
.git/
.gitignore
.markdownlint.json
setup.sh
install.zsh
restore.zsh
stow.yaml
tests/
packages/
scripts/
os/
macos-defaults.yaml
.claude/
